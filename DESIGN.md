# LRC Video Maker — 设计方案

## 1. 项目概述

**定位**: 将音频、LRC 双语歌词、专辑封面三要素合成为专业水准的卡拉OK/动态歌词视频（MP4，1920×1080@60fps）。Gui采用pyside6

**目标用户**: 音乐爱好者、翻唱创作者、内容制作者——无需专业视频编辑技能即可一键生成歌词视频。

---

## 2. 功能模块

| 功能 | 描述 |
|------|------|
| 文件输入 | 加载音频 (.mp3/.wav/.flac/.m4a)、封面图 (.jpg/.png/.webp)、LRC 歌词、可选独立背景图 |
| 工程管理 | `.kproj` JSON 工程文件的保存/加载，版本号兼容 (v1.1) |
| 实时预览 | 拖动时间轴到任意时间点，生成该时间点的静态画面预览 |
| 智能色彩提取 | KMeans 聚类从封面提取主色/辅色/描边色，基于亮度、饱和度、对比度的智能选色 |
| 背景动画 | 静态模糊、渐变波浪（生成式）、波浪模糊（基于图片） |
| 歌词动画 | 淡入淡出（单行高亮）、滚动列表（多行高亮+缓动滚动） |
| 封面动画 | 静态展示（含倒影）、黑胶唱片旋转（含纹理+高光） |
| 硬件加速 | NVIDIA NVENC / AMD AMF / Intel QSV，回退到软件编码 libx264 |

---

## 3. 技术栈

---

## 4. 设计思路

### 4.1 统一参数对象

所有生成参数封装为单一数据对象 VideoGenParams，作为界面层与处理层之间的**唯一契约**。界面收集当前控件值后打包为该对象，处理层仅依赖此对象，不直接引用任何界面控件。

### 4.2 异步任务模型

视频渲染和预览均为耗时操作，通过 QThread 后台线程执行，避免 UI 冻结。线程内创建日志桥接对象，将 FFmpeg 实时输出通过 Qt 信号槽机制回传至界面层的日志框和进度条。

### 4.3 FFmpeg 滤镜图策略

视频合成的核心是一条复杂的滤镜链（filter_complex），写入临时脚本文件通过参数传入，避免命令行转义问题。滤镜链的构建思路：

1. **背景层**：对输入图片或纯数学生成器应用背景动画滤镜
2. **封面层**：对封面图应用封面动画滤镜
3. **合成层**：将背景与封面按黄金比例布局叠加
4. **文字层**：在合成画面上叠加歌词动画滤镜
5. **输出**：附加音频流，编码为最终 MP4

### 4.4 性能优化策略

- **低分辨率生成再放大**：波浪/渐变等生成式动画在缩小的画布上计算，再高质量放大到目标尺寸
- **超采样抗锯齿**：黑胶唱片动画使用 8× 超采样，中间分辨率极高，最终缩回目标尺寸获得平滑边缘
- **歌词窗口裁剪**：预览模式仅渲染当前时间点附近的少量歌词行，而非全部歌词
- **脚本文件传参**：复杂滤镜通过临时文件传入 FFmpeg，而非命令行内联，避免长度限制和转义错误

### 4.5 动画可扩展机制

动画效果通过字典注册表管理——每类动画（背景/歌词/封面）维护一个名称到生成函数的映射表。新增动画只需编写滤镜生成函数并注册到对应字典，界面下拉框自动出现新选项。

### 4.6 组件注入式 UI

界面控件由工厂函数集中构建，构建完成后直接赋值到主窗口对象上。主窗口业务逻辑通过属性名访问控件。这是一种隐式耦合——添加新控件时需同步维护两侧的属性名。

### 4.7 智能色彩提取

从封面图提取三色（主色/辅色/描边色）的流程：

1. 缩放封面至 150×150 降低计算量
2. KMeans 聚类提取 10 个颜色簇
3. 筛选亮度 20%-80%、饱和度 >25% 的候选色
4. 主色：候选中占比最高者
5. 次色：与主色对比度最高的簇
6. 描边色：与主色对比度最高且非次色的簇
7. 降级：无合适候选时回退到最饱和色或黑白

### 4.8 双语 LRC 解析

支持两种双语歌词格式：

| 格式 | 说明 |
|------|------|
| 时间戳重复 | 同一时间戳出现两次，每行各一种语言 |
| 斜杠分隔 | 单行内用 `/` 分隔两种语言 |

同时提取 [ti:]、[ar:]、[al:] 等元数据标签，用于自动生成默认文件名。

---

## 5. 布局策略

歌词区域采用**黄金比例布局**：

- 封面居左，占据画面宽度的黄金分割窄侧（约 W/2.618）
- 歌词居右，位于剩余宽侧空间的中心
- 封面 Y 轴垂直居中

---

## 6. 视频输出规格

| 参数 | 值 |
|------|---|
| 分辨率 | 1920 × 1080 (Full HD) |
| 帧率 | 60 fps |
| 视频编码 | libx264（默认）/ h264_nvenc / h264_amf / h264_qsv |
| 音频编码 | AAC 320kbps |
| 像素格式 | yuv420p |

## 

## 8. 自定义参数说明

### 8.1 文件输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| 音频文件 | 文件路径 | 支持 .mp3 / .wav / .flac / .m4a |
| 封面图片 | 文件路径 | 支持 .jpg / .jpeg / .png / .webp |
| LRC 歌词 | 文件路径 | 支持标准 LRC 格式（含双语） |
| 背景图片 | 文件路径（可选） | 未设置时自动使用封面作为背景 |

### 8.2 歌词样式参数

| 参数 | 类型 | 说明 |
|------|------|------|
| 主歌词字体 | 字体文件名 | 从 font/ 文件夹自动扫描 |
| 主歌词字号 | 整数 (10-300) | 像素单位 |
| 主歌词颜色 | 十六进制颜色 | 纯白 |
| 次要歌词字体 | 字体文件名 | 从 font/ 文件夹自动扫描 |
| 次要歌词字号 | 整数 (10-300) | 像素单位 |
| 次要歌词颜色 | 十六进制颜色 | 浅灰 |
| 描边颜色 | 十六进制颜色 | 纯黑 |
| 描边宽度 | 整数 (0-20) | 像素单位，设为 0 则无描边 |

### 8.3 动画选择参数

| 参数 | 可选值 | 说明 |
|------|--------|------|
| 背景动画 | 静态模糊 / 渐变波浪 / 波浪模糊 | 渐变波浪为纯数学生成，不依赖图片输入 |
| 歌词动画 | 淡入淡出 / 滚动列表 | 淡入淡出为单行居中，滚动列表为多行滚动高亮 |
| 封面动画 | 静态展示 / 唱片旋转 | 静态展示带柔和倒影，唱片旋转模拟黑胶唱片 |

### 8.4 高级设置参数

| 参数 | 可选值 | 说明 |
|------|--------|------|
| FFmpeg 路径 | 文件路径 | 默认从系统 PATH 查找，可手动指定完整路径 |
| 硬件加速 | 无 (软件编码 x264) / NVIDIA / AMD / Intel | 选择与显卡匹配的编码器以加速渲染 |

### 8.5 预览控制参数

| 参数 | 类型 | 说明 |
|------|------|------|
| 预览时间点 | 滑块 (0 ~ 音频时长) | 精度 0.01 秒，拖动选择要预览的时间点 |

## 
